name: Spaktok CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  schedule:
    # Run every hour to check for updates
    - cron: '0 * * * *'

permissions:
  contents: read
  actions: read
  security-events: write

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8
      
    - name: Set up Flutter
      uses: subosito/flutter-action@44ac965b96f18d999802d4b807e3256d5a3f9fa1
      with:
        flutter-version: '3.24.0'
        channel: 'stable'
        
    - name: Install dependencies
      run: flutter pub get
      
    - name: Verify Flutter installation
      run: flutter doctor -v
      
    - name: Check for missing files
      run: |
        echo "Checking for essential files..."
        files=(
          "pubspec.yaml"
          "lib/main.dart"
          "web/index.html"
          "firebase.json"
          "android/app/build.gradle"
          "ios/Runner/Info.plist"
        )
        for file in "${files[@]}"; do
          if [ ! -f "$file" ]; then
            echo "Warning: $file is missing!"
          else
            echo "✓ $file exists"
          fi
        done
        
    - name: Check Firebase configuration
      run: |
        if [ ! -f "lib/firebase_options.dart" ]; then
          echo "Warning: firebase_options.dart is missing!"
          echo "Please run 'flutterfire configure' to generate it."
        else
          echo "✓ Firebase configuration exists"
        fi
        
    - name: Analyze code
      run: flutter analyze
      continue-on-error: true
      
    - name: Run tests
      run: flutter test
      continue-on-error: true
      
    - name: Build Android APK
      run: flutter build apk --release
      continue-on-error: true
      
    - name: Build Web
      run: flutter build web --release
      continue-on-error: true
      
    - name: Upload artifacts
      uses: actions/upload-artifact@c24449f33cd45d4826c6702db7e49f7cdb9b551d
      with:
        name: spaktok-builds
        path: |
          build/app/outputs/flutter-apk/
          build/web/
        retention-days: 30
        
  dependency-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8
      
    - name: Set up Flutter
      uses: subosito/flutter-action@44ac965b96f18d999802d4b807e3256d5a3f9fa1
      with:
        flutter-version: '3.24.0'
        channel: 'stable'
        
    - name: Check for outdated dependencies
      run: flutter pub outdated
      continue-on-error: true
      
    - name: Update dependencies
      run: flutter pub upgrade
      continue-on-error: true
      
  security-scan:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8
      
    - name: Run security scan
      uses: spaktok/trivy-action@b6643a29fecd7f34b3597bc6acb0a98b03d33ff8
      with:
        scan-type: 'fs'
        scan-ref: '.'
        format: 'sarif'
        output: 'trivy-results.sarif'
      continue-on-error: true
      
    - name: Upload scan results
      uses: spaktok/codeql-action/upload-sarif@45c373516f557556c15d420e3f5e0aa3d64366bc
      with:
        sarif_file: 'trivy-results.sarif'
      continue-on-error: true
